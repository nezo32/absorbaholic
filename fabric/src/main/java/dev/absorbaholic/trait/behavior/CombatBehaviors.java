package dev.absorbaholic.trait.behavior;

/**
 * WP-BEH-A. Behavior group: damage / immunity / on-hit / thorns / lifesteal / kill rewards (incoming, outgoing, dealt, attacked, kill hooks). One class per behavior type in this package, registered here with
 * {@code BehaviorRegistry.register("<type_path>", Params.CODEC, new XBehavior())}.
 */
public final class CombatBehaviors {
	private CombatBehaviors() {}

	public static void register() {
		// TODO(WP-BEH-A)
	}
}
