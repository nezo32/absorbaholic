package dev.absorbaholic.test;

import net.fabricmc.fabric.api.client.gametest.v1.FabricClientGameTest;
import net.fabricmc.fabric.api.client.gametest.v1.context.ClientGameTestContext;

/**
 * WP-CW (+ WP-CLIENT / WP-UI sections). Client gametest (runClientGameTest, needs a display): the Create World button
 * sits between Difficulty and Allow Cheats, defaults ON, toggles, reaches settings.dat; notification handling;
 * the traits screen opens. Skeleton: does nothing yet.
 */
public class AbsorbClientGameTest implements FabricClientGameTest {
	@Override
	public void runTest(ClientGameTestContext ctx) {
		// TODO(WP-CW)
	}
}
